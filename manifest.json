[
  {
    "guid": "ced1b4c4-47d1-4cd5-8d22-1491f69676ef",
    "name": "Media Converter",
    "description": "Browse your Jellyfin library, pick a video, and convert it to a different container/codec using the server's own bundled ffmpeg - automatically using whichever hardware transcoder (Intel QSV, Nvidia NVENC, AMD AMF) is already configured in Jellyfin's playback settings, or CPU if none is enabled - either replacing the original file in place or creating a new variant alongside it, with a live progress bar.",
    "overview": "Convert library media files from the dashboard, using this server's configured ffmpeg hardware transcoder or CPU.",
    "owner": "rangoDJ",
    "category": "General",
    "versions": [
      {
        "version": "1.0.29.0",
        "changelog": "1.0.29: Searching the library no longer lists every matching episode individually - an episode match now collapses into its parent show, so a search like \"shameless\" returns one \"Shameless\" result (use \"Browse episodes\" to drill into seasons) instead of a long flat list of every episode whose title happened to match. The season episode list also gets checkboxes and a \"Convert selected episodes\" button, so you can convert an arbitrary subset instead of only all-or-one. 1.0.28: Fixed \"Remove all completed jobs\"/\"Remove all history\" appearing to silently do nothing - any failure was updating an error banner at the top of the page while those buttons sit at the bottom, so it went unseen; the banner now scrolls into view, and one job failing to remove no longer blocks every other eligible job in the batch from being removed too. The season list (via \"Browse episodes\") now sorts seasons numerically instead of by incidental server order, and season 0 is labeled \"Specials\" instead of \"Season 0\". 1.0.27: The jobs section is now an actual table (File/Status/ Progress/Actions columns) instead of a loose stack of divs. Added \"Select all pending\", \"Remove all completed jobs\", and \"Remove all history\" for tidying up the list in bulk (jobs still awaiting a variant decision are always left alone so their extra file isn't orphaned). Also added a \"Pause queue after current job\" toggle: whatever's currently converting is left to finish, but no further queued jobs start until you resume - already-queued jobs stay queued rather than being cancelled. 1.0.25: Jobs awaiting a variant keep/delete decision now have a checkbox, and two new buttons above the jobs list let you apply \"Keep as new variant\" or \"Keep as original\" to every checked job at once, instead of resolving them one at a time. 1.0.24: The two comparison players now stay in sync instead of drifting apart - seeking the original automatically moves the new variant to the same position, and a \"Sync to original's time\" button corrects drift during normal playback. 1.0.23: Fixed side-by-side comparison playback for real this time - passing the auth token via query string (1.0.22) still got a 401, since Jellyfin's elevation policy doesn't grant elevated access from a query-string token the way it does from the request header normal API calls use, and a <video src> load has no way to send that header. The stream endpoints now validate a short-lived, single-purpose token minted by the plugin itself (handed out only through a normal authenticated request) instead of relying on Jellyfin's own per-request auth for the streaming requests. 1.0.22: Fixed side-by-side comparison playback actually not loading at all - the video element's src had no auth token, and since a <video src> load can't carry the request header our other API calls use, the server was rejecting it before it ever reached the plugin's code (explaining both the stuck players and the total absence of server-side log lines). The access token is now passed explicitly as a query parameter. 1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.29/jellyfin-media-converter_1.0.29.0.zip",
        "checksum": "66e26351951b46fdfd5cf2dfd0239844",
        "timestamp": "2026-08-17T16:39:34Z"
      },
      {
        "version": "1.0.28.0",
        "changelog": "1.0.28: Fixed \"Remove all completed jobs\"/\"Remove all history\" appearing to silently do nothing - any failure was updating an error banner at the top of the page while those buttons sit at the bottom, so it went unseen; the banner now scrolls into view, and one job failing to remove no longer blocks every other eligible job in the batch from being removed too. The season list (via \"Browse episodes\") now sorts seasons numerically instead of by incidental server order, and season 0 is labeled \"Specials\" instead of \"Season 0\". 1.0.27: The jobs section is now an actual table (File/Status/ Progress/Actions columns) instead of a loose stack of divs. Added \"Select all pending\", \"Remove all completed jobs\", and \"Remove all history\" for tidying up the list in bulk (jobs still awaiting a variant decision are always left alone so their extra file isn't orphaned). Also added a \"Pause queue after current job\" toggle: whatever's currently converting is left to finish, but no further queued jobs start until you resume - already-queued jobs stay queued rather than being cancelled. 1.0.25: Jobs awaiting a variant keep/delete decision now have a checkbox, and two new buttons above the jobs list let you apply \"Keep as new variant\" or \"Keep as original\" to every checked job at once, instead of resolving them one at a time. 1.0.24: The two comparison players now stay in sync instead of drifting apart - seeking the original automatically moves the new variant to the same position, and a \"Sync to original's time\" button corrects drift during normal playback. 1.0.23: Fixed side-by-side comparison playback for real this time - passing the auth token via query string (1.0.22) still got a 401, since Jellyfin's elevation policy doesn't grant elevated access from a query-string token the way it does from the request header normal API calls use, and a <video src> load has no way to send that header. The stream endpoints now validate a short-lived, single-purpose token minted by the plugin itself (handed out only through a normal authenticated request) instead of relying on Jellyfin's own per-request auth for the streaming requests. 1.0.22: Fixed side-by-side comparison playback actually not loading at all - the video element's src had no auth token, and since a <video src> load can't carry the request header our other API calls use, the server was rejecting it before it ever reached the plugin's code (explaining both the stuck players and the total absence of server-side log lines). The access token is now passed explicitly as a query parameter. 1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.28/jellyfin-media-converter_1.0.28.0.zip",
        "checksum": "4b4bfafe8f4035b2e0e76a2b8c8ccd36",
        "timestamp": "2026-08-17T16:21:21Z"
      },
      {
        "version": "1.0.27.0",
        "changelog": "1.0.27: The jobs section is now an actual table (File/Status/ Progress/Actions columns) instead of a loose stack of divs. Added \"Select all pending\", \"Remove all completed jobs\", and \"Remove all history\" for tidying up the list in bulk (jobs still awaiting a variant decision are always left alone so their extra file isn't orphaned). Also added a \"Pause queue after current job\" toggle: whatever's currently converting is left to finish, but no further queued jobs start until you resume - already-queued jobs stay queued rather than being cancelled. 1.0.25: Jobs awaiting a variant keep/delete decision now have a checkbox, and two new buttons above the jobs list let you apply \"Keep as new variant\" or \"Keep as original\" to every checked job at once, instead of resolving them one at a time. 1.0.24: The two comparison players now stay in sync instead of drifting apart - seeking the original automatically moves the new variant to the same position, and a \"Sync to original's time\" button corrects drift during normal playback. 1.0.23: Fixed side-by-side comparison playback for real this time - passing the auth token via query string (1.0.22) still got a 401, since Jellyfin's elevation policy doesn't grant elevated access from a query-string token the way it does from the request header normal API calls use, and a <video src> load has no way to send that header. The stream endpoints now validate a short-lived, single-purpose token minted by the plugin itself (handed out only through a normal authenticated request) instead of relying on Jellyfin's own per-request auth for the streaming requests. 1.0.22: Fixed side-by-side comparison playback actually not loading at all - the video element's src had no auth token, and since a <video src> load can't carry the request header our other API calls use, the server was rejecting it before it ever reached the plugin's code (explaining both the stuck players and the total absence of server-side log lines). The access token is now passed explicitly as a query parameter. 1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.27/jellyfin-media-converter_1.0.27.0.zip",
        "checksum": "6aad19a19c6a47f1f4b62033470c8f13",
        "timestamp": "2026-08-16T22:47:36Z"
      },
      {
        "version": "1.0.25.0",
        "changelog": "1.0.25: Jobs awaiting a variant keep/delete decision now have a checkbox, and two new buttons above the jobs list let you apply \"Keep as new variant\" or \"Keep as original\" to every checked job at once, instead of resolving them one at a time. 1.0.24: The two comparison players now stay in sync instead of drifting apart - seeking the original automatically moves the new variant to the same position, and a \"Sync to original's time\" button corrects drift during normal playback. 1.0.23: Fixed side-by-side comparison playback for real this time - passing the auth token via query string (1.0.22) still got a 401, since Jellyfin's elevation policy doesn't grant elevated access from a query-string token the way it does from the request header normal API calls use, and a <video src> load has no way to send that header. The stream endpoints now validate a short-lived, single-purpose token minted by the plugin itself (handed out only through a normal authenticated request) instead of relying on Jellyfin's own per-request auth for the streaming requests. 1.0.22: Fixed side-by-side comparison playback actually not loading at all - the video element's src had no auth token, and since a <video src> load can't carry the request header our other API calls use, the server was rejecting it before it ever reached the plugin's code (explaining both the stuck players and the total absence of server-side log lines). The access token is now passed explicitly as a query parameter. 1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.25/jellyfin-media-converter_1.0.25.0.zip",
        "checksum": "956b50800f80b0a58338c1c38f12bfc0",
        "timestamp": "2026-08-16T17:48:58Z"
      },
      {
        "version": "1.0.24.0",
        "changelog": "1.0.24: The two comparison players now stay in sync instead of drifting apart - seeking the original automatically moves the new variant to the same position, and a \"Sync to original's time\" button corrects drift during normal playback. 1.0.23: Fixed side-by-side comparison playback for real this time - passing the auth token via query string (1.0.22) still got a 401, since Jellyfin's elevation policy doesn't grant elevated access from a query-string token the way it does from the request header normal API calls use, and a <video src> load has no way to send that header. The stream endpoints now validate a short-lived, single-purpose token minted by the plugin itself (handed out only through a normal authenticated request) instead of relying on Jellyfin's own per-request auth for the streaming requests. 1.0.22: Fixed side-by-side comparison playback actually not loading at all - the video element's src had no auth token, and since a <video src> load can't carry the request header our other API calls use, the server was rejecting it before it ever reached the plugin's code (explaining both the stuck players and the total absence of server-side log lines). The access token is now passed explicitly as a query parameter. 1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.24/jellyfin-media-converter_1.0.24.0.zip",
        "checksum": "f508356b949a61f8fa2bcc5f07ff783a",
        "timestamp": "2026-08-16T17:44:03Z"
      },
      {
        "version": "1.0.23.0",
        "changelog": "1.0.23: Fixed side-by-side comparison playback for real this time - passing the auth token via query string (1.0.22) still got a 401, since Jellyfin's elevation policy doesn't grant elevated access from a query-string token the way it does from the request header normal API calls use, and a <video src> load has no way to send that header. The stream endpoints now validate a short-lived, single-purpose token minted by the plugin itself (handed out only through a normal authenticated request) instead of relying on Jellyfin's own per-request auth for the streaming requests. 1.0.22: Fixed side-by-side comparison playback actually not loading at all - the video element's src had no auth token, and since a <video src> load can't carry the request header our other API calls use, the server was rejecting it before it ever reached the plugin's code (explaining both the stuck players and the total absence of server-side log lines). The access token is now passed explicitly as a query parameter. 1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.23/jellyfin-media-converter_1.0.23.0.zip",
        "checksum": "ff4370d11236d8b6d2c0166c589e31fd",
        "timestamp": "2026-08-16T17:34:06Z"
      },
      {
        "version": "1.0.22.0",
        "changelog": "1.0.22: Fixed side-by-side comparison playback actually not loading at all - the video element's src had no auth token, and since a <video src> load can't carry the request header our other API calls use, the server was rejecting it before it ever reached the plugin's code (explaining both the stuck players and the total absence of server-side log lines). The access token is now passed explicitly as a query parameter. 1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.22/jellyfin-media-converter_1.0.22.0.zip",
        "checksum": "5efc56d93725b1cd8af18377fcf932aa",
        "timestamp": "2026-08-16T17:18:41Z"
      },
      {
        "version": "1.0.21.0",
        "changelog": "1.0.21: Fixed \"Failed to execute 'json' on 'Response': Unexpected end of JSON input\" when clicking Cancel, Keep new variant, or Keep original - those actions return an empty 204 response, but the dashboard was forcing a JSON parse on it regardless, surfacing a false error even though the action had already succeeded server-side. 1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.21/jellyfin-media-converter_1.0.21.0.zip",
        "checksum": "4ce6726af54526514cc08f73e43f2106",
        "timestamp": "2026-08-16T17:03:13Z"
      },
      {
        "version": "1.0.20.0",
        "changelog": "1.0.20: \"Half of each source's current bitrate\" is now a proper rate control option in the convert dialog, usable for season/series batch conversions too - each item's target bitrate is resolved from its own current bitrate individually, not a single shared value. The inline media stats line now shows video/audio bitrate. Finished jobs can now be removed from the jobs list. 1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.20/jellyfin-media-converter_1.0.20.0.zip",
        "checksum": "271976bf943a5d5914aba933cad16144",
        "timestamp": "2026-08-16T05:17:25Z"
      },
      {
        "version": "1.0.18.0",
        "changelog": "1.0.18: Added detailed logging across the dashboard API and the job lifecycle, visible in the Jellyfin console/logs: the full ffmpeg/ ffprobe command line for every conversion and preview remux, conversion progress every 10%, and a trace of each request (Convert, Compare, Stream, job status transitions, history save/ load). Diagnostic groundwork for tracking down cases where comparison preview playback still doesn't work. 1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.18/jellyfin-media-converter_1.0.18.0.zip",
        "checksum": "f55a9939929e276d43784eb485e2a8b3",
        "timestamp": "2026-08-16T05:01:13Z"
      },
      {
        "version": "1.0.16.0",
        "changelog": "1.0.16: Fixed comparison preview playback still not working for files whose audio codec (DTS, TrueHD, etc.) ffmpeg's MP4 muxer refuses to stream-copy - the 1.0.15 remux was failing outright for exactly those files and silently falling back to the original unplayable file. Now retries a video-only remux (audio dropped) before giving up, so video plays even when the source audio codec can't be copied into an MP4 container. 1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.16/jellyfin-media-converter_1.0.16.0.zip",
        "checksum": "de1d7ea9ca77492bf16cddba7488bb2d",
        "timestamp": "2026-08-16T04:40:59Z"
      },
      {
        "version": "1.0.15.0",
        "changelog": "1.0.15: The side-by-side comparison videos now autoplay when the popup opens. Sources in non-browser-friendly containers (e.g. Matroska) are now transparently remuxed to MP4 via ffmpeg stream copy before preview - no re-encoding, byte-identical streams, just a container repackage so the browser can open the file, cached to disk so seeking still works. Audio codecs no browser can decode (DTS, TrueHD, etc.) remain silent regardless, since that's a browser limitation this can't fix without actually re-encoding audio - noted directly in the comparison popup. 1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.15/jellyfin-media-converter_1.0.15.0.zip",
        "checksum": "7ecee18712fc95801d0ca2b368287879",
        "timestamp": "2026-08-16T04:34:14Z"
      },
      {
        "version": "1.0.14.0",
        "changelog": "1.0.14: Job history now survives a server/container restart instead of disappearing (each job is persisted to disk as it changes, and reloaded on startup; anything still queued/running when the server stopped is marked as interrupted rather than silently resumed). The \"Compare original vs. new variant\" button now sits inline in the job's row instead of a separate panel below it. \"Play side by side\" now opens in a popup overlay on top of the page rather than inline - clicking outside it or its Close button stops playback and closes it. Side-by-side playback streams the original/new files directly (no transcoding), so quality always matches the source exactly. 1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.14/jellyfin-media-converter_1.0.14.0.zip",
        "checksum": "545f7e40c0236d1ebfc3e42adb17a777",
        "timestamp": "2026-08-16T04:10:17Z"
      },
      {
        "version": "1.0.13.0",
        "changelog": "1.0.13: Fixed the variant comparison panel silently collapsing a few seconds after opening it - the jobs list was fully rebuilding its DOM on every 3-second poll even when nothing had changed, wiping out the comparison results. Jobs are now only re-rendered when their status/progress/variant decision actually changes. Also added a \"Play side by side\" option to the variant comparison view, streaming the original and new variant files into two video players with a shared restart/pause control, so you can visually compare playback before deciding which one to keep. 1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.13/jellyfin-media-converter_1.0.13.0.zip",
        "checksum": "7e10545a35d7015bdcfc79572af89582",
        "timestamp": "2026-08-16T03:55:57Z"
      },
      {
        "version": "1.0.11.0",
        "changelog": "1.0.11: Clicking an episode/movie's name now shows its full ffprobe codec/bitrate info (previously only a compact summary with no bitrate). The convert dialog can now target an average video bitrate instead of only a fixed quality value, with an optional max bitrate cap, and shows a predicted output file size when doing so. A \"Convert at ~half bitrate\" shortcut appears for HEVC/AV1 when the source's bitrate is known. Running jobs now show an actual progress bar instead of only a percentage number. 1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.11/jellyfin-media-converter_1.0.11.0.zip",
        "checksum": "b5bdcf266e76ea95ab4003541f4da73b",
        "timestamp": "2026-08-16T03:20:20Z"
      },
      {
        "version": "1.0.10.0",
        "changelog": "1.0.10: The series episode browser now lists seasons first (with episode counts) instead of dumping every episode from every season into one long scrolling list. Click a season to see just its episodes, with a \"Back to seasons\" step before returning to the search results - much more usable for shows with many episodes. 1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.10/jellyfin-media-converter_1.0.10.0.zip",
        "checksum": "60c3a8043df6690316358b7e2ecd2217",
        "timestamp": "2026-08-15T18:13:22Z"
      },
      {
        "version": "1.0.9.0",
        "changelog": "1.0.9: The convert dialog now exposes audio codec/bitrate, subtitle handling (copy/drop), output resolution scaling, and encoder preset/speed, instead of always copying audio/subtitles and only letting you choose container, codec, and quality. You can also convert an entire season or series in one action instead of one item at a time. Each movie/episode row now shows its codec, resolution, audio, and file size, read live via ffprobe rather than Jellyfin's own library metadata. \"Create new variant\" jobs let you compare the original and new file's ffprobe stats side by side once finished, then choose which one to keep/delete. Conversions are now rejected up front with a clear message if the destination folder isn't writable by the server process, instead of failing partway through the job. 1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.9/jellyfin-media-converter_1.0.9.0.zip",
        "checksum": "67ac954ec1e45d78928be7616f166966",
        "timestamp": "2026-08-15T17:31:14Z"
      },
      {
        "version": "1.0.8.0",
        "changelog": "1.0.8: Fixed conversion progress staying frozen at 0% for the whole job. Progress calculation was silently skipped whenever the library item's RunTimeTicks metadata was missing (common for freshly-added or partially-scanned episodes) - now falls back to probing the file's actual duration via ffprobe when that metadata isn't present. 1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.8/jellyfin-media-converter_1.0.8.0.zip",
        "checksum": "2c9a40f908aad246c88005f701862182",
        "timestamp": "2026-08-14T23:08:55Z"
      },
      {
        "version": "1.0.7.0",
        "changelog": "1.0.7: Actually fixed the dashboard page's script never executing. Root-caused against a local Jellyfin 12.0.0 instance by reading the web client's own bundle: it extracts only div[data-role=\"page\"] from the fetched HTML (via innerHTML, discarding everything outside that div) and only re-executes <script> tags found inside it (via jQuery) when present. Our script was a sibling AFTER that div, so it was silently dropped before ever reaching the DOM. Moving it to be the last child *inside* #mediaConverterPage fixes it - verified live: Search, Convert, the episode picker, and Jobs polling all work now. 1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.7/jellyfin-media-converter_1.0.7.0.zip",
        "checksum": "1b0dd8e76e1b1a225b7f7907e2f3bc48",
        "timestamp": "2026-08-14T22:59:05Z"
      },
      {
        "version": "1.0.6.0",
        "changelog": "1.0.6: Confirmed Jellyfin's config-page loader strips <script> tags entirely (inline or external) on some web client versions, so the dashboard page's JS may never run regardless of how it's referenced. Adds itemdetail.js, a standalone script for use with a global script injector plugin (e.g. JavaScript Injector), which adds a floating Convert button directly on movie/episode detail pages. 1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.6/jellyfin-media-converter_1.0.6.0.zip",
        "checksum": "6c6ff62465e6225d5549e10df135f750",
        "timestamp": "2026-08-14T22:44:07Z"
      },
      {
        "version": "1.0.5.0",
        "changelog": "1.0.5: The dashboard's JavaScript is now served as its own embedded page and loaded via a <script src> reference, instead of being inlined directly in the config page's HTML - inline scripts weren't reliably executing on some Jellyfin web client versions (12.0-rc5), which was the actual root cause behind the button clicks silently doing nothing even after the 1.0.4 fix. 1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.5/jellyfin-media-converter_1.0.5.0.zip",
        "checksum": "2389580bda6e284ab1b8f86382855a59",
        "timestamp": "2026-08-14T22:33:27Z"
      },
      {
        "version": "1.0.4.0",
        "changelog": "1.0.4: Fixed Search/Convert/Cancel going dead again after navigating back to the Media Converter page within the same browser session - a one-time init guard was skipping listener setup on freshly inserted DOM elements after the first page visit. 1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.4/jellyfin-media-converter_1.0.4.0.zip",
        "checksum": "36e32fd419e9a1e47096d008d89d21ff",
        "timestamp": "2026-08-14T21:52:44Z"
      },
      {
        "version": "1.0.3.0",
        "changelog": "1.0.3: Searching now matches series names too, showing an episode picker (ordered by season/episode) instead of requiring the exact episode title. The dashboard also surfaces API errors visibly instead of failing silently. 1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.3/jellyfin-media-converter_1.0.3.0.zip",
        "checksum": "05c32cf83a0dc55eef80944c12d4d900",
        "timestamp": "2026-08-14T21:33:54Z"
      },
      {
        "version": "1.0.2.0",
        "changelog": "1.0.2: Fixed the plugin always reporting itself as version 0.0.0.0, which made Jellyfin's plugin details page fail with \"An error occurred while getting the plugin details from the repository.\" 1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.2/jellyfin-media-converter_1.0.2.0.zip",
        "checksum": "d606e7a95df7cb9d43b960efbf2ded93",
        "timestamp": "2026-08-14T21:23:53Z"
      },
      {
        "version": "1.0.1.0",
        "changelog": "1.0.1: Fixed the dashboard page's Search/Convert/Cancel buttons never responding to clicks - a pageshow event guard was checking the wrong DOM node and always bailing out before wiring up any listeners. 1.0.0: Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.1/jellyfin-media-converter_1.0.1.zip",
        "checksum": "9bd33fa57e14e918609894ef69144c0a",
        "timestamp": "2026-08-14T21:19:32Z"
      },
      {
        "version": "1.0.0.0",
        "changelog": "Initial release: dashboard library browser, convert dialog with curated presets plus an advanced raw ffmpeg-args override, replace/variant modes, and live job progress.",
        "targetAbi": "12.0.0.0",
        "sourceUrl": "https://github.com/rangoDJ/jellyfin-media-converter/releases/download/v1.0.0/jellyfin-media-converter_1.0.0.zip",
        "checksum": "9dfa6fb791a01e2d4a932008f2bd47e0",
        "timestamp": "2026-08-14T21:10:52Z"
      }
    ]
  }
]
